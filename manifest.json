{
  "name": "path_foundation_mcp",
  "description": "MCP wrapper for Google's Path Foundation model on ModelScope",
  "version": "1.0",
  "input_schema": {
    "type": "object",
    "properties": {
      "image_url": {
        "type": "string",
        "description": "URL of pathology image to analyze"
      }
    },
    "required": ["image_url"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "embedding": {
        "type": "array",
        "items": {"type": "number"},
        "description": "Generated feature embedding from Path Foundation model"
      }
    }
  }
}
